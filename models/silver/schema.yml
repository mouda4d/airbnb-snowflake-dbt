version: 2
models:
  - name: silver_hosts
    description: "Cleaned host's data - null values replaced with 'Anonymous' for host_name"
  - name: silver_listings
    description: "Cleaned listings data - only the most recent record for each listing_id is kept"
  - name: silver_bookings
    description: "Cleaned bookings data - only the most recent record for each booking_id is kept"