version: 2
models:
  - name: bronze_hosts
    description: "Raw hosts data, passed through unchanged from the RAW source. Incrementally loaded; existing host_id rows are updated in place rather than duplicated."
  - name: bronze_listings
    description: "Raw listings data, passed through unchanged from the RAW source. Incrementally loaded; existing listing_id rows are updated in place rather than duplicated."
  - name: bronze_bookings
    description: "Raw bookings data, passed through unchanged from the RAW source. Incrementally loaded; existing booking_id rows are updated in place rather than duplicated."